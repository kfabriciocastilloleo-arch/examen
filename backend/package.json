{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'Running backend tests...'",
    "build": "mkdir -p dist && echo 'Building backend...' > dist/index.js",
    "lint": "echo 'Linting backend...'"
  }
}
