name: production
url: https://production.example.com
deployment_branch: main
protection_rules:
  required_reviewers:
    - devops-leads
    - security-team
  wait_timer: 10
  required_status_checks:
    - ci-cd-pipeline
secrets:
  - PROD_API_KEY
